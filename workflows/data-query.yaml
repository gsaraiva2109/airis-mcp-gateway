name: data-query
description: "Use Supabase for database operations"
priority: high
max_tokens: 80
servers:
  - supabase

trigger: "querying, modifying, or analyzing database data"

compile_to: |
  ### Database Operations
  WHEN querying, modifying, or analyzing database data:
  1. Call supabase:query with the appropriate SQL statement
