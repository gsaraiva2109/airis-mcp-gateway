name: web-research
description: "Use web search for current information and best practices"
priority: high
max_tokens: 120
servers:
  - tavily

trigger: "need current information, external data, or best practices"

compile_to: |
  ### Web Research
  WHEN you need current information, external data, or best practices:
  1. Call tavily:tavily-search with a focused search query
  2. Synthesize results before proceeding with implementation
